# --- Database ---
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/payments_dashboard"

# --- Provider Endpoints & Keys (replace with real) ---
PROVIDER_LIVE_BASE_URL="https://api.provider.com"
PROVIDER_SANDBOX_BASE_URL="https://sandbox.api.provider.com"
PROVIDER_LIVE_API_KEY="live_key_here"
PROVIDER_SANDBOX_API_KEY="sandbox_key_here"

# The base URL your clients will call (docs/SDKs etc.)
PUBLIC_API_BASE_URL="http://localhost:3000/api/v1"

# Webhook shared secret with provider
WEBHOOK_SECRET="replace_me"
